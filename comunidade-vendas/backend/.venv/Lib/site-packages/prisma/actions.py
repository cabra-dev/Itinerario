# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse
from ._constants import CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED

if TYPE_CHECKING:
    from .client import Prisma
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class ProdutoActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Produto]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Produto.prisma().query_raw(
            'SELECT * FROM Produto WHERE id = $1',
            1103527590,
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Produto
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Produto.prisma().query_first(
            'SELECT * FROM Produto WHERE nome = $1',
            'dhheabfhf',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.ProdutoCreateInput,
        include: Optional[types.ProdutoInclude] = None
    ) -> _PrismaModelT:
        """Create a new Produto record.

        Parameters
        ----------
        data
            Produto record data
        include
            Specifies which relations should be loaded on the returned Produto model

        Returns
        -------
        prisma.models.Produto
            The created Produto record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Produto record from just the required fields
        produto = await Produto.prisma().create(
            data={
                # data to create a Produto record
                'nome': 'ggciceaie',
                'categoria': 'bbehjachib',
                'preco': 2035015474.36880,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.ProdutoCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Produto records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Produto record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Produto.prisma().create_many(
            data=[
                {
                    # data to create a Produto record
                    'nome': 'bfaiacjjfc',
                    'categoria': 'eigcfgbif',
                    'preco': 1062517886.26783,
                },
                {
                    # data to create a Produto record
                    'nome': 'biabhbdai',
                    'categoria': 'idghgaicb',
                    'preco': 595337866.79042,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.ProdutoWhereUniqueInput,
        include: Optional[types.ProdutoInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Produto record.

        Parameters
        ----------
        where
            Produto filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Produto model

        Returns
        -------
        prisma.models.Produto
            The deleted Produto record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        produto = await Produto.prisma().delete(
            where={
                'id': 2111915288,
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.ProdutoWhereUniqueInput,
        include: Optional[types.ProdutoInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Produto record.

        Parameters
        ----------
        where
            Produto filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Produto model

        Returns
        -------
        prisma.models.Produto
            The found Produto record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        produto = await Produto.prisma().find_unique(
            where={
                'id': 1149758321,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.ProdutoWhereUniqueInput,
        include: Optional[types.ProdutoInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Produto record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Produto filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Produto model

        Returns
        -------
        prisma.models.Produto
            The found Produto record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        produto = await Produto.prisma().find_unique_or_raise(
            where={
                'id': 1644289366,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProdutoWhereInput] = None,
        cursor: Optional[types.ProdutoWhereUniqueInput] = None,
        include: Optional[types.ProdutoInclude] = None,
        order: Optional[Union[types.ProdutoOrderByInput, List[types.ProdutoOrderByInput]]] = None,
        distinct: Optional[List[types.ProdutoScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Produto records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Produto records returned
        skip
            Ignore the first N results
        where
            Produto filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Produto model
        order
            Order the returned Produto records by any field
        distinct
            Filter Produto records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Produto]
            The list of all Produto records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Produto records
        produtos = await Produto.prisma().find_many(take=10)

        # find the first 5 Produto records ordered by the categoria field
        produtos = await Produto.prisma().find_many(
            take=5,
            order={
                'categoria': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ProdutoWhereInput] = None,
        cursor: Optional[types.ProdutoWhereUniqueInput] = None,
        include: Optional[types.ProdutoInclude] = None,
        order: Optional[Union[types.ProdutoOrderByInput, List[types.ProdutoOrderByInput]]] = None,
        distinct: Optional[List[types.ProdutoScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Produto record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Produto filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Produto model
        order
            Order the returned Produto records by any field
        distinct
            Filter Produto records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Produto
            The first Produto record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Produto record ordered by the estoque field
        produto = await Produto.prisma().find_first(
            skip=1,
            order={
                'estoque': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ProdutoWhereInput] = None,
        cursor: Optional[types.ProdutoWhereUniqueInput] = None,
        include: Optional[types.ProdutoInclude] = None,
        order: Optional[Union[types.ProdutoOrderByInput, List[types.ProdutoOrderByInput]]] = None,
        distinct: Optional[List[types.ProdutoScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Produto record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Produto filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Produto model
        order
            Order the returned Produto records by any field
        distinct
            Filter Produto records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Produto
            The first Produto record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Produto record ordered by the vendido field
        produto = await Produto.prisma().find_first_or_raise(
            skip=1,
            order={
                'vendido': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.ProdutoUpdateInput,
        where: types.ProdutoWhereUniqueInput,
        include: Optional[types.ProdutoInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Produto record.

        Parameters
        ----------
        data
            Produto record data specifying what to update
        where
            Produto filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Produto model

        Returns
        -------
        prisma.models.Produto
            The updated Produto record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        produto = await Produto.prisma().update(
            where={
                'id': 1388290519,
            },
            data={
                # data to update the Produto record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.ProdutoWhereUniqueInput,
        data: types.ProdutoUpsertInput,
        include: Optional[types.ProdutoInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Produto filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Produto model

        Returns
        -------
        prisma.models.Produto
            The created or updated Produto record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        produto = await Produto.prisma().upsert(
            where={
                'id': 1647418052,
            },
            data={
                'create': {
                    'id': 1647418052,
                    'nome': 'biabhbdai',
                    'categoria': 'idghgaicb',
                    'preco': 595337866.79042,
                },
                'update': {
                    'nome': 'biabhbdai',
                    'categoria': 'idghgaicb',
                    'preco': 595337866.79042,
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.ProdutoUpdateManyMutationInput,
        where: types.ProdutoWhereInput,
    ) -> int:
        """Update multiple Produto records

        Parameters
        ----------
        data
            Produto data to update the selected Produto records to
        where
            Filter to select the Produto records to update

        Returns
        -------
        int
            The total number of Produto records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Produto records
        total = await Produto.prisma().update_many(
            data={
                'preco': 1675546029.176727
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProdutoWhereInput] = None,
        cursor: Optional[types.ProdutoWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Produto records present in the database

        Parameters
        ----------
        select
            Select the Produto fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Produto filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ProdutoCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Produto.prisma().count()

        # results: prisma.types.ProdutoCountAggregateOutput
        results = await Produto.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.ProdutoCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProdutoWhereInput] = None,
        cursor: Optional[types.ProdutoWhereUniqueInput] = None,
    ) -> types.ProdutoCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.ProdutoCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProdutoWhereInput] = None,
        cursor: Optional[types.ProdutoWhereUniqueInput] = None,
    ) -> Union[int, types.ProdutoCountAggregateOutput]:
        """Count the number of Produto records present in the database

        Parameters
        ----------
        select
            Select the Produto fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Produto filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ProdutoCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Produto.prisma().count()

        # results: prisma.types.ProdutoCountAggregateOutput
        results = await Produto.prisma().count(
            select={
                '_all': True,
                'nome': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ProdutoCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.ProdutoWhereInput] = None
    ) -> int:
        """Delete multiple Produto records.

        Parameters
        ----------
        where
            Optional Produto filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Produto records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Produto records
        total = await Produto.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.ProdutoScalarFieldKeys'],
        *,
        where: Optional['types.ProdutoWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ProdutoAvgAggregateInput'] = None,
        sum: Optional['types.ProdutoSumAggregateInput'] = None,
        min: Optional['types.ProdutoMinAggregateInput'] = None,
        max: Optional['types.ProdutoMaxAggregateInput'] = None,
        having: Optional['types.ProdutoScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ProdutoCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ProdutoScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ProdutoScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ProdutoGroupByOutput']:
        """Group Produto records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Produto fields to group records by
        where
            Produto filter to select records
        take
            Limit the maximum number of Produto records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ProdutoGroupByOutput]
            A list of dictionaries representing the Produto record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Produto records by categoria values
        # and count how many records are in each group
        results = await Produto.prisma().group_by(
            ['categoria'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class VendaActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Venda]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Venda.prisma().query_raw(
            'SELECT * FROM Venda WHERE id = $1',
            326272115,
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Venda
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Venda.prisma().query_first(
            'SELECT * FROM Venda WHERE produtoId = $1',
            1343201072,
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.VendaCreateInput,
        include: Optional[types.VendaInclude] = None
    ) -> _PrismaModelT:
        """Create a new Venda record.

        Parameters
        ----------
        data
            Venda record data
        include
            Specifies which relations should be loaded on the returned Venda model

        Returns
        -------
        prisma.models.Venda
            The created Venda record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Venda record from just the required fields
        venda = await Venda.prisma().create(
            data={
                # data to create a Venda record
                'produtoId': 675780521,
                'quantidade': 744964398,
                'valorTotal': 1969681615.111617,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.VendaCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Venda records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Venda record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Venda.prisma().create_many(
            data=[
                {
                    # data to create a Venda record
                    'produtoId': 861472101,
                    'quantidade': 1303003706,
                    'valorTotal': 1686638315.200043,
                },
                {
                    # data to create a Venda record
                    'produtoId': 1868141281,
                    'quantidade': 1860847622,
                    'valorTotal': 1448521415.162865,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.VendaWhereUniqueInput,
        include: Optional[types.VendaInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Venda record.

        Parameters
        ----------
        where
            Venda filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Venda model

        Returns
        -------
        prisma.models.Venda
            The deleted Venda record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        venda = await Venda.prisma().delete(
            where={
                'id': 1249606685,
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.VendaWhereUniqueInput,
        include: Optional[types.VendaInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Venda record.

        Parameters
        ----------
        where
            Venda filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Venda model

        Returns
        -------
        prisma.models.Venda
            The found Venda record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        venda = await Venda.prisma().find_unique(
            where={
                'id': 835903122,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.VendaWhereUniqueInput,
        include: Optional[types.VendaInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Venda record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Venda filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Venda model

        Returns
        -------
        prisma.models.Venda
            The found Venda record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        venda = await Venda.prisma().find_unique_or_raise(
            where={
                'id': 763719779,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.VendaWhereInput] = None,
        cursor: Optional[types.VendaWhereUniqueInput] = None,
        include: Optional[types.VendaInclude] = None,
        order: Optional[Union[types.VendaOrderByInput, List[types.VendaOrderByInput]]] = None,
        distinct: Optional[List[types.VendaScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Venda records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Venda records returned
        skip
            Ignore the first N results
        where
            Venda filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Venda model
        order
            Order the returned Venda records by any field
        distinct
            Filter Venda records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Venda]
            The list of all Venda records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Venda records
        vendas = await Venda.prisma().find_many(take=10)

        # find the first 5 Venda records ordered by the quantidade field
        vendas = await Venda.prisma().find_many(
            take=5,
            order={
                'quantidade': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.VendaWhereInput] = None,
        cursor: Optional[types.VendaWhereUniqueInput] = None,
        include: Optional[types.VendaInclude] = None,
        order: Optional[Union[types.VendaOrderByInput, List[types.VendaOrderByInput]]] = None,
        distinct: Optional[List[types.VendaScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Venda record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Venda filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Venda model
        order
            Order the returned Venda records by any field
        distinct
            Filter Venda records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Venda
            The first Venda record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Venda record ordered by the valorTotal field
        venda = await Venda.prisma().find_first(
            skip=1,
            order={
                'valorTotal': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.VendaWhereInput] = None,
        cursor: Optional[types.VendaWhereUniqueInput] = None,
        include: Optional[types.VendaInclude] = None,
        order: Optional[Union[types.VendaOrderByInput, List[types.VendaOrderByInput]]] = None,
        distinct: Optional[List[types.VendaScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Venda record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Venda filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Venda model
        order
            Order the returned Venda records by any field
        distinct
            Filter Venda records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Venda
            The first Venda record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Venda record ordered by the dataVenda field
        venda = await Venda.prisma().find_first_or_raise(
            skip=1,
            order={
                'dataVenda': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.VendaUpdateInput,
        where: types.VendaWhereUniqueInput,
        include: Optional[types.VendaInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Venda record.

        Parameters
        ----------
        data
            Venda record data specifying what to update
        where
            Venda filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Venda model

        Returns
        -------
        prisma.models.Venda
            The updated Venda record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        venda = await Venda.prisma().update(
            where={
                'id': 429995104,
            },
            data={
                # data to update the Venda record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.VendaWhereUniqueInput,
        data: types.VendaUpsertInput,
        include: Optional[types.VendaInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Venda filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Venda model

        Returns
        -------
        prisma.models.Venda
            The created or updated Venda record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        venda = await Venda.prisma().upsert(
            where={
                'id': 1775811865,
            },
            data={
                'create': {
                    'id': 1775811865,
                    'produtoId': 1868141281,
                    'quantidade': 1860847622,
                    'valorTotal': 1448521415.162865,
                },
                'update': {
                    'produtoId': 1868141281,
                    'quantidade': 1860847622,
                    'valorTotal': 1448521415.162865,
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.VendaUpdateManyMutationInput,
        where: types.VendaWhereInput,
    ) -> int:
        """Update multiple Venda records

        Parameters
        ----------
        data
            Venda data to update the selected Venda records to
        where
            Filter to select the Venda records to update

        Returns
        -------
        int
            The total number of Venda records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Venda records
        total = await Venda.prisma().update_many(
            data={
                'id': 893145566
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.VendaWhereInput] = None,
        cursor: Optional[types.VendaWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Venda records present in the database

        Parameters
        ----------
        select
            Select the Venda fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Venda filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.VendaCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Venda.prisma().count()

        # results: prisma.types.VendaCountAggregateOutput
        results = await Venda.prisma().count(
            select={
                '_all': True,
                'produtoId': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.VendaCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.VendaWhereInput] = None,
        cursor: Optional[types.VendaWhereUniqueInput] = None,
    ) -> types.VendaCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.VendaCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.VendaWhereInput] = None,
        cursor: Optional[types.VendaWhereUniqueInput] = None,
    ) -> Union[int, types.VendaCountAggregateOutput]:
        """Count the number of Venda records present in the database

        Parameters
        ----------
        select
            Select the Venda fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Venda filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.VendaCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Venda.prisma().count()

        # results: prisma.types.VendaCountAggregateOutput
        results = await Venda.prisma().count(
            select={
                '_all': True,
                'quantidade': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.VendaCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.VendaWhereInput] = None
    ) -> int:
        """Delete multiple Venda records.

        Parameters
        ----------
        where
            Optional Venda filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Venda records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Venda records
        total = await Venda.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.VendaScalarFieldKeys'],
        *,
        where: Optional['types.VendaWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.VendaAvgAggregateInput'] = None,
        sum: Optional['types.VendaSumAggregateInput'] = None,
        min: Optional['types.VendaMinAggregateInput'] = None,
        max: Optional['types.VendaMaxAggregateInput'] = None,
        having: Optional['types.VendaScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.VendaCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.VendaScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.VendaScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.VendaGroupByOutput']:
        """Group Venda records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Venda fields to group records by
        where
            Venda filter to select records
        take
            Limit the maximum number of Venda records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.VendaGroupByOutput]
            A list of dictionaries representing the Venda record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Venda records by valorTotal values
        # and count how many records are in each group
        results = await Venda.prisma().group_by(
            ['valorTotal'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models